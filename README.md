npm run migration:generate -d src/infrastructure/database/migrations/create-table.
Make build before running migrations

EDA IMPLEMENTATION
CQRS MODULE IMPLEMENTATION
PROBLEM DETAILS RFC
LOCKING IN UPDATE PATIENT
